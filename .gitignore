node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# iOS (generated by `cap add ios`, built in CI)
ios/App/Pods/
ios/App/build/
ios/App/App.xcworkspace/xcuserdata/
ios/App/App.xcodeproj/xcuserdata/
*.ipa

# Release signing material - never commit
*.keystore
*.jks
keystore.properties
release-keystore-credentials.txt

# Exception: fixed debug-only keystore, intentionally committed so every CI
# build gets the same SHA-1 (well-known debug password/alias, not sensitive)
!android/app/ci-debug.keystore
